---
date: 2019-03-16T11:04:02Z
title: "jx create client openapi"
slug: jx_create_client_openapi
url: /commands/jx_create_client_openapi/
---
## jx create client openapi

Creates OpenAPI specs for Custom Resources

### Synopsis

This command code generates OpenAPI specs for the specified custom resources.

```
jx create client openapi [flags]
```

### Examples

```
  # lets generate client docs
  jx create client openapi
  --output-package=github.com/jenkins-x/jx/pkg/client \
  --input-package=github.com/jenkins-x/pkg-apis \
  --group-with-version=jenkins.io:v1
  --version=1.2.3
  --title=Jenkins X
  
  # You will normally want to add a target to your Makefile that looks like:
  
  generate-openapi:
  jx create client openapi
  --output-package=github.com/jenkins-x/jx/pkg/client \
  --input-package=github.com/jenkins-x/jx/pkg/apis \
  --group-with-version=jenkins.io:v1
  --version=${VERSION}
  --title=${TITLE}
  
  # and then call:
  
  make generate-openapi
```

### Options

```
      --boilerplate-file string            Custom boilerplate to add to all files if the file is missing it will be ignored (default "custom-boilerplate.go.txt")
  -g, --group-with-version stringArray     group name:version (e.g. jenkins.io:v1) to generate, must specify at least once
  -h, --help                               help for openapi
      --input-base string                  Input base (the root of module the OpenAPI is being generated for), by default the current working directory (default "/workspace/jx-docs/content/commands")
  -i, --input-package string               Input package (relative to input base), must specify
      --module-name string                 module name (e.g. github.com/jenkins-x/jx) (default "workspace/jx-docs/content/commands")
      --open-api-dependency stringArray    Add <path:package:group:apiVersion> dependencies for OpenAPI generation (default [k8s.io/apimachinery:pkg/apis:meta:v1,k8s.io/apimachinery:pkg/api:resource:,k8s.io/apimachinery:pkg/util:intstr:,k8s.io/api::batch:v1,k8s.io/api::core:v1,k8s.io/api::rbac:v1])
      --openapi-generator-version string   Version (really a commit-ish) of github.com/kubernetes/kube-openapi (default "ced9eb3070a5f1c548ef46e8dfe2a97c208d9f03")
      --openapi-output-directory string    Output directory for the OpenAPI specs, relative to the output-base unless absolute. OpenAPI spec JSON and YAML files are placed in openapi-spec sub directory. (default "docs/apidocs")
      --output-base string                 Output base directory, by default the current working directory (default "/workspace/jx-docs/content/commands")
  -o, --output-package string              Output package, must specify
      --title string                       Title for OpenAPI, JSON Schema and HTML docs (default "Jenkins X")
      --version string                     Version for OpenAPI, JSON Schema and HTML docs
```

### Options inherited from parent commands

```
  -b, --batch-mode                Runs in batch mode without prompting for user input
      --install-dependencies      Enables automatic dependencies installation when required
      --log-level string          Sets the logging level (panic, fatal, error, warning, info, debug) (default "info")
      --no-brew                   Disables brew package manager on MacOS when installing binary dependencies
      --skip-auth-secrets-merge   Skips merging the secrets from local files with the secrets from Kubernetes cluster
      --verbose                   Enables verbose output
```

### SEE ALSO

* [jx create client](/commands/jx_create_client/)	 - Creates clients for Custom Resources

###### Auto generated by spf13/cobra on 16-Mar-2019
